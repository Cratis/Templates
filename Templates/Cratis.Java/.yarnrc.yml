# Yarn 2 and later install with Plug'n'Play by default, which the TypeScript 7 compiler
# used by the frontend build cannot resolve. Install into node_modules instead.
# npm, pnpm and Yarn 1 ignore this file.
nodeLinker: node-modules
